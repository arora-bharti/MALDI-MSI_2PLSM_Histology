# MALDI-MSI_2PLSM_Histology dependencies
# Install with: pip install -r requirements.txt
#
# Versions are pinned to the combination this project was verified against
# (Python 3.10, macOS arm64 and Linux x86_64). Use requirements-dev.txt as well
# if you want to run the notebooks or the test suite.

# Core scientific computing
numpy==2.2.6
pandas==2.3.3
scipy==1.15.3

# Image processing
Pillow==12.3.0
opencv-python-headless==5.0.0.93
scikit-image==0.25.2
tifffile==2025.5.10

# Machine learning
scikit-learn==1.7.2

# Deep learning backend for StarDist.
# Chosen per platform on purpose:
#   - macOS (Apple Silicon): tensorflow-cpu publishes NO arm64 wheel, so the
#     plain tensorflow package is required.
#   - Linux (Docker, Streamlit Community Cloud): plain tensorflow pulls in
#     ~2 GB of nvidia-* CUDA packages that are useless without a GPU and will
#     blow the Streamlit Cloud resource limit. tensorflow-cpu is ~273 MB.
tensorflow==2.21.0; platform_system == "Darwin"
tensorflow-cpu==2.21.0; platform_system != "Darwin"

# Nuclei segmentation
stardist==0.9.2
csbdeep==0.8.2

# Visualization
matplotlib==3.10.9

# Progress bars
tqdm==4.70.0

# Interactive web app
streamlit==1.61.1

# MALDI-MSI analysis (open-source SCiLS Lab replacement)
pyimzml==1.5.5
shapely==2.1.2

# Spatial nuclei network analysis
networkx==3.4.2
